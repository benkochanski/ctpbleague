# Exclude static-hosting files — these are Firebase/web assets, not GAS scripts
public/**
dill-dinkers/**
scorecard/**
little-league-app/**

# Cloudflare Worker code (ES modules, not GAS)
worker/**

# Exclude local tooling and config
.claude/**
.DS_Store
*.py
*.tsv
*.json
!appsscript.json
